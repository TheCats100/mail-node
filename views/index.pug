extends layout

block content
  h1= title
  p Welcome to #{title}
  form(method="POST" enctype="multipart/form-data" action="/monupload")
    input(type="file" name="monfichier" multiple accept=".png")
    button(type="submit" value="Upload Photo") envoyer
  
